export const postsList = ({
  postsList
}) => postsList.postsList

export const headline = ({
  headline
}) => headline.headline

export const post = ({
  post
}) => post.post

export const tags = ({
  tags
}) => tags.tagSet

export const tagPostsList = ({
  tagPostsList
}) => tagPostsList.tagPostsList

export const tag = ({
  tag
}) => tagPostsList.tag
